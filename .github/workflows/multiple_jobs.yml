name: multiple

on:
  push:
    branches: [ master ]

jobs:
  test:
     runs-on: ubuntu-latest
     steps:
      - run: echo "testing..."

  deploy:
    runs-on: ubuntu-latest
    needs: test 
    steps: 
      - run: echo "deploying..."

